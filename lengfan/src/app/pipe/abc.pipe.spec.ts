/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AbcPipe } from './abc.pipe';

describe('AbcPipe', () => {
  it('create an instance', () => {
    const pipe = new AbcPipe();
    expect(pipe).toBeTruthy();
  });
});
